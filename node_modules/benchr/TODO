- promises : true optie

suite([NAME, ]function() {

  before(function([done]) { ... });
  after (function([done]) { ... });

  beforeEach(function([done]) { ... });
  afterEach (function([done]) { ... });

  completed(function([done]) { ... });

  benchmark(NAME, function([done]) { ... });
  benchmark(NAME, function([done]) { ... });
  benchmark(NAME, function([done]) { ... });
});

- suite(function() { ... }) (zonder naam) werkt niet
